# 고무팔 · 대항해 — 작업 인수인계

아래 내용을 새 대화에 붙여넣으면 이어서 작업할 수 있습니다.

---

## 먼저 할 일

**bash에서 저장소 원본을 직접 받는 편이 낫습니다.** `github.io`는 bash 네트워크 허용
목록에 없지만 `raw.githubusercontent.com`은 허용되어 있어서, web_fetch로 70KB를
컨텍스트에 밀어 넣지 않고 바로 디스크에 받을 수 있습니다.

```bash
curl -s -o index.html   https://raw.githubusercontent.com/aechamjae/Gomugomu/main/index.html
curl -s -o manifest.json https://raw.githubusercontent.com/aechamjae/Gomugomu/main/manifest.json
curl -s -o sw.js         https://raw.githubusercontent.com/aechamjae/Gomugomu/main/sw.js
```

배포본은 `https://aechamjae.github.io/Gomugomu/index.html` 입니다.

파일 하나에 HTML·CSS·JS가 전부 들어 있는 약 70KB짜리 단일 파일입니다.
**전체가 `(function(){"use strict"; … })();` IIFE로 감싸여 있습니다** — 검증 훅을
붙일 때 이 점이 중요합니다(아래 "검증 방법" 참고).

외부 라이브러리를 하나도 쓰지 않습니다. **오디오 관련 코드는 의도적으로 전혀 없습니다**
(`Audio`, `AudioContext`, `<audio>`, 진동 API 모두 미사용). 이 원칙은 계속 지켜 주세요.

---

## 게임 개요

바다 위 돛대 사이를 고무처럼 늘어나는 팔로 건너가는 사이드스크롤 스윙 게임입니다.
스윙 액션 게임의 조작감(진자 펌핑, 줄 감기, 급강하 가속, 체공 보너스)을 참고했고,
해적 세계관으로 꾸몄습니다.

- 목표: 거리(m)를 늘리기. 바다에 빠지거나, 화면 뒤로 밀려나거나, 해왕류에 닿으면 종료
- 보조 점수: 보물(금화 수집 + 체공 보너스 + 보스 격침 + 잡몹 처치)

## 조작

| 입력 | 동작 |
|---|---|
| 마우스/Space **누르고 있기** | 가장 가까운 앞쪽 고리에 팔이 걸림. 떼면 발사 |
| 방향키 ← → | 매달렸을 땐 진자 펌핑(접선 방향), 공중에선 방향 조절 |
| 방향키 ↓ | 매달렸을 땐 줄 감아 오르기, 공중에선 급강하 가속 |
| 방향키 ↑ | 도약하며 놓기 |
| **Q** | **고무고무 피스톨** (원거리, 5초) |
| **W** | **고무고무 엘리펀트 건** (광역 즉발, 15초) |
| P / Esc | 일시정지 (창을 벗어나면 자동 정지) |
| R | 다시 시작 |

베리어베리어(E)·돌진(D) 스킬은 삭제했다. WASD는 예전엔 방향키 보조 바인딩
(A=←, S=↓, D=→, W=↑)이었는데, W를 스킬 키로 넘기면서 방향키 전용으로 정리했다
(A/S/D는 여전히 좌/하/우 보조키로 남아 있음). 이동은 방향키만 써도 아무 문제 없다.

터치 기기에서는 전체화면 레이아웃으로 바뀌고 화면 버튼이 나타납니다.
왼쪽 아래 ◀▶, 그 위로 아래에서부터 "피스톨"→"건" 버튼이 쌓여 있고,
오른쪽 아래 큰 "팔" 버튼과 ▲▼, 오른쪽 위 일시정지. 세로로 들면 "가로로 돌려주세요"
안내가 뜬다.

## 물리 상수 (현재 값)

```js
const W = 900, H = 540;
const SEA = 486;          // 바다 표면 y
const GRAV   = 0.36;
const K1     = 0.040;     // 고무 기본 장력
const K2     = 0.0018;    // 늘어날수록 뻣뻣해지는 비선형 항
const DAMP   = 0.085;     // 반경 방향(늘었다 줄었다) 감쇠
const STRETCH= 1.40;      // 최대 신장 배율 — 넘으면 위치를 구속
const REEL   = 0.30;      // 자동 감기
const REEL_MIN = 74;      // 자동 감기 하한
const ZIP    = 1.15;      // ↓ 로 감을 때 (46까지)
const GRAB_CD= 10;        // 팔 재사용 대기 프레임
const REACH  = 310;       // 팔이 닿는 거리
const AIR    = 0.150;     // 공중 방향 조절
const PUMP   = 0.115;     // 진자 펌핑
const DIVE   = 0.30;      // 급강하 가속
const MAXV   = 27;

/* 고무고무 피스톨 */
const SKILL_CD  = 60*5;   // 재충전 5초 (20초의 1/4) — 시작할 땐 충전된 상태
const FIST_SPD  = 33;
const FIST_LIFE = 26;     // 약 860px 사거리
const FIST_R    = 22;

/* 고무고무 엘리펀트 건 — 광역 강타, G키 */
const GUN_CD    = 60*15;  // 재충전 15초 (60초의 1/4) — 시작할 땐 충전된 상태
const GUN_R     = 150;    // 충격 반경
const GUN_LIFE  = 34;

/* 잡몹 */
const KING_H    = 300;    // 해왕류가 솟구치는 높이
const KING_W    = 52;     // 목 굵기(반지름)
```

**걸 때 줄 길이**는 체공 시간에 비례합니다.
`pull = 0.99 - 0.09 * min(1, airTime/20)` — 오래 날아온 뒤 걸면 몸이 당겨 올라가고,
놓자마자 다시 걸면 거의 당겨지지 않습니다. 연타 방지 장치입니다.

## 지형 생성

- 고리 높이: 처음엔 높게(y 96) 시작해 1300m 지점에서 낮게(y 214)까지 내려감
- 간격: 176px에서 시작해 1500m에서 280px까지, **상한 340px**
- 줄 길이는 `SEA - 52 - 고리높이`로 제한해 스윙 바닥이 바다에 닿지 않게 함
- 고리를 하나 만들 때마다 `spawnMobs(x0, x1)`로 그 구간에 잡몹을 뿌림

## 잡몹

`mobs` 배열로 관리하고 `updateMobs(dt)` / `drawMobs()`가 담당합니다.
보스와는 완전히 별개 시스템입니다.

### 식인 물고기 (`kind: 0`)

- 90m부터, 구간마다 확률 0.20(400m 미만) ~ 0.30, 한 번에 1~3마리 떼
- 물속에 잠복하다 플레이어가 앞쪽 330px 안에 들어오면 튀어오름 (`vy = -(7.6~10.4)`)
- 닿으면 **가벼운 휘청** — `knock(false, true)`, 물고기는 그대로 죽음
- 판정 반경 15px

### 해왕류 (`kind: 1`)

- 520m부터, 직전 출현으로부터 2600px 이상 떨어져야 하고 확률 **0.11**(예전 0.34의 1/3.
  "빈도가 너무 잦고 사기다"는 피드백으로 줄임)
- 5단계: `phase 0` 잠복(물 밑 그림자) → `1` 경고 → `2` 솟구침 → `3` 유지(150프레임) → `4` 잠수
- **경고(phase 1)는 100m 앞(`gap < 2200px`)에서 미리 시작**하고, 실제로 솟구치는 건
  (phase 1→2) 아주 가까워졌을 때(`gap < 260px`)로 분리했다. 예전엔 `gap < 660px`(30m)에서
  경고가 뜨고 고작 46프레임 뒤 바로 솟구쳐서 갑툭튀처럼 느껴졌다(플레이어 피드백으로 발견)
- 처음엔 작은 월드 앵커 팝업(`pops`)으로 경고했는데 "너무 작아서 안 보인다"는 피드백을
  받아, `drawKingWarning()`으로 교체 — 화면 상단 고정 위치(카메라 스케일 밖)에 큰 배너로
  "⚠ 해왕류 접근 중 — 전방 Nm"를 phase 1 내내(100m→12m) 실시간 갱신해서 보여준다.
  보스가 떠 있으면 보스 체력바 아래로 위치를 내려서 겹치지 않게 함
- **목·머리 어디든 닿으면 `knockKing()`** — 보스는 몸으로 부딪혀도 안 죽는데 해왕류만
  즉사인 게 형평에 안 맞는다는 피드백으로, 즉사(`die("해왕류")`)를 없애고 아주 강한
  스턴으로 바꿨다: `stun=70, iframes=90, p.vx*=0.22`(보스 근접 hard-knock의 stun 42,
  속도 0.34배보다 훨씬 세다), 보물 40 손실. 죽지는 않지만 크게 휘청이다 보니 그 여파로
  바다에 빠질 수는 있다 — 그건 즉사가 아니라 자연스러운 후폭풍. "해왕류에게 삼켜졌다"
  사망 화면 문구는 이제 안 쓰여서 제거함
- 목은 세로 캡슐(`segToCapsule`), 머리는 반지름 74 원으로 판정
- 잠복→경고(phase 1) 전환 시 화면에 "⚠ 해왕류 접근! 피스톨/엘리펀트 건으로 물리쳐라" 경고 문구가 뜬다
- **피스톨이나 엘리펀트 건에 맞으면 그 자리에서 완전히 처치된다**(`m.gone = true`,
  피스톨 +220 / 엘리펀트 건 +260 보물). 예전엔 밀어내기만 하고 죽이지 않아 "타파법"이
  없었는데, 이제 원거리 스킬이 실질적인 대응 수단이 됐다

### 휘청의 세 단계

```js
knock(hard, mild)
// hard  — 크라켄 촉수 등: 스턴 42, 속도 0.34배
// 기본  — 보스 약점에 느리게 닿거나 탄 피격: 스턴 30, 속도 0.52배
// mild  — 잡몹에 스침: 스턴 20, 속도 0.74배, 무적 60프레임
```

## 고무고무 피스톨

- 키보드 **Q** (예전엔 F/E/Shift였다가 4스킬 체계로 개편하며 Q로 정리)
- 쿨타임 `SKILL_CD`(**5초**, 2분→20초→5초로 두 번 단축함). **시작할 땐 충전된 상태**라
  첫 발은 항상 쓸 수 있음
- `pistolAim()`이 앞쪽 860px 안의 보스 약점 / 공중의 물고기 / 솟은 해왕류 중
  가장 가까운 것을 자동 조준. 목표가 없으면 정면으로
- 주먹이 뻗었다가 되돌아옴(`fist.back`). 되돌아오는 동안엔 판정 없음
- 효과: 날아오는 탄을 쳐냄 / 물고기 즉사(+15) / **해왕류 완전 처치(+220)** /
  **보스는 속도 조건 없이 약점 명중**
- 캔버스 좌하단에 쿨타임 링(`drawSkillGauge`), 터치 버튼(`#skillBtn`)엔 남은 초 표시

쿨타임 20초라 한 판(대개 40~90초) 안에 여러 번 쓸 수 있게 됐고, 잡몹·해왕류를
상시 처리하는 "기본 무기" 역할을 한다.

## 고무고무 엘리펀트 건

- 키보드 **W** (예전엔 G였다가 4스킬 체계로 개편하며 W로 정리), 터치는 피스톨 버튼
  위의 보라색 "건" 버튼(`#gunBtn`)
- 쿨타임 `GUN_CD`(**15초**, 2분→60초→15초로 두 번 단축함). 시작할 땐 충전된 상태
- 조준이 필요 없는 **광역 즉발기**. 플레이어 정면(진행 방향) 200px 앞을 중심으로
  반경 `GUN_R`(150px) 안의 모든 대상을 즉시 타격
  - 식인 물고기: 반경 안이면 전부 즉사(+15)
  - 해왕류: 반경+74px 안이면 완전 처치(+260)
  - 보스: 반경+40px 안에 있으면(약점 여부·무적 상관없이) `damageBoss()` 1회 적중
- 사거리가 없는 대신 판정이 넓고 조건이 관대함 — "정확한 원거리 딜"보다는
  "위급할 때 근처를 정리하는 묵직한 한 방"에 가깝다
- 시각효과는 보라색 충격파 원(`drawGun`), HUD엔 피스톨 게이지 위에 두 번째 게이지(`drawGunGauge`)

> 플레이어가 "스킬을 쓰면 줄이 끊긴다"고 느낀 적이 있어 코드를 전부 훑어봤는데,
> `firePistol()`/`fireGun()`/`damageBoss(ranged=true, ...)` 어디에도 `rope = null`이
> 없다 — 스킬 자체는 줄을 끊지 않는다. 줄을 끊는 건 오직 `release()`, `knock()`,
> `damageBoss(ranged=false, ...)`(몸으로 직접 부딪힌 근접 판정), 최대 신장 초과뿐이다.
> 보스 약점에 몸으로 스칠 정도로 가까이 붙어서 스킬을 쏘면, 스킬과는 별개로
> 근접 판정(`updateBoss()` 끝부분)이 같은 프레임에 동시에 발동해 끊긴 것처럼
> 보였을 가능성이 높다 — 이건 원래부터 있던 근접 전투 규칙이라 건드리지 않았다.

베리어베리어(방어, E)와 돌진(전방 가속+강타, D) 스킬은 삭제했다.
`fireBarrier`/`fireDash`/`updateBarrier`/`updateDash`/`drawBarrier`/`drawDash`/
`drawBarrierGauge`/`drawDashGauge`와 관련 상수·상태(`barrier`, `barrierCd`,
`dash`, `dashCd`)를 전부 제거했다.

## 보스

500m마다 중간보스, 2000m마다 대형 보스(크라켄).
예전엔 직전에 나온 중간보스 종류를 연속으로 제외했는데, 이제는 거리 조건만
충족되면 직전과 같은 종류라도 그대로 나온다(`spawnBoss`가 4종 중 완전
무작위로 뽑음).

**명중 조건은 속도 8 이상입니다** (이전 문서에 32로 적혀 있었으나 코드는 계속 8이었음).
느리게 닿거나 탄에 맞으면 휘청 + 이후 78프레임 무적.

### 약점은 여러 개고 전부 움직입니다

`weakPoints(b)`가 배열을 돌려줍니다(예전 `weakPoint()` 단수 함수는 없어졌습니다).
각 항목은 `{x, y, r, mark}` 이고 `mark`는 명중 팝업에 표시됩니다.

| # | 이름 | 특징 | 약점 |
|---|---|---|---|
| 0 | 곡예사 선장 | 줄무늬 기구를 타고 단검을 3갈래로 던짐 | **기구**(좌우로 흔들림) + **곡예사**(그 아래 진자 운동, `b.swing`) |
| 1 | 작살잡이 | 물속에 잠겨 있다 앞쪽에서 솟구침 | **몸통**(공중에 뜬 동안) + **꼬리**(솟구쳐 오르는 동안만) |
| 2 | 포함 흑조호 | 아래에서 포탄을 포물선으로 발사. 뒤처지면 **잠항해서 따라붙음** | **화약통**(도르래로 46px 오르내림) + **예비통**(갑판 뒤, 낮고 위험). 잠항 중엔 약점 없음 |
| 3 | 밧줄 끊는 자 | 바로 다음 고리에 앉아 그 고리를 끊음 | **몸통** + **늘어뜨린 밧줄 끝 갈고리**(좌우로 흔들림) |
| 4 | 심해의 크라켄 (대형) | 부상 ↔ 잠수 2단계, HP 3 이하면 격노 | 부상 중엔 **눈**(두리번거림), **잠수 중엔 숨구멍**(150프레임 주기로 62프레임 열림) |

크라켄은 잠수 중에도 숨구멍으로 반격할 수 있습니다(예전엔 잠수 중 아예 불가능했음).

중간보스 HP 2, 크라켄 HP 8. 예전엔 중간보스 40초·크라켄 95초 제한시간이 지나면
퇴각했는데, 이제 둘 다 시간으로는 사라지지 않고 물리치거나(중간보스는 지나쳐도)
사라질 때까지 계속 남아 있습니다(`boss.life` 타이머와 퇴각 로직 제거).
보스 이동 속도에는 **절대 상한**이 있고(8.0~9.8), 240px 이상 뒤처지면 앞쪽으로 재배치됩니다.
곡예사·작살잡이·크라켄은 아직 순간이동식 재배치입니다.

### 흑조호 잠항 (`b.dive`, `b.sink`)

흑조호만 순간이동을 쓰지 않습니다. 200px 이상 뒤처지면:

`dive 1` 잠항(sink 168까지) → `dive 2` 물밑 고속 이동 → `dive 3` 부상 → `dive 0` 복귀(70프레임 뒤 재사격)

잠항 중에는 조준·사격이 멈추고 `weakPoints()`가 빈 배열을 돌려줍니다(`sink > 12`).

물밑 속도는 반드시 **플레이어보다 빠르게** 잡아야 합니다.

```js
b.x += Math.max(17, Math.abs(p.vx) + 7) * dt;
```

고정값 17로 두면 빠른 플레이어(MAXV 27)를 영영 못 따라잡아 부상 단계에 진입하지 못하고,
제한시간이 끝날 때까지 물속에 잠긴 채로 사라집니다. 실제로 겪은 버그입니다.

## 해결한 버그들 (같은 실수 반복 방지)

1. `draw()` 안에서 `len`을 두 번 `const` 선언 → 스크립트 전체 파싱 실패, 빈 화면
2. 보스가 플레이어 속도를 그대로 복사해 영원히 못 잡음 → 속도 상한으로 해결
3. 탄이 현재 위치를 조준해 전부 뒤로 흘러감 → 도달 시간 역산 예측 사격
4. 마주 달리면 한 프레임에 20px 넘게 상대 이동해 판정 원을 건너뜀 → 한 프레임의 이동 경로(선분) 전체로 판정
5. 촉수에 연속 피격되어 크라켄에 반격 불가 → 피격 후 무적 프레임 도입
6. 자동 감기가 고리 코앞(46px)까지 당겨 진자가 소멸하고 상하 진동만 남음 → 하한 74px
7. 쿨타임 중 버튼을 누르고 있으면 끝나도 안 걸림 → 선입력(누르고 있으면 자동 발동)
8. iOS는 data URI 방식 `apple-touch-icon`을 무시 → 호스팅 번들은 실제 파일 참조
9. 잡몹 첫 도입 시 물고기가 너무 잦아 평균 거리가 3분의 1로 폭락 → 출현 확률 절반,
   떼 규모 축소, 잡몹 전용 `mild` 휘청 신설
10. 검증 훅을 스크립트 **끝**에 붙이면 IIFE 바깥이라 내부 변수에 접근 불가 →
    `})();` **앞**에 끼워 넣어야 함
11. 카메라가 고속 주행 중 `cam.z`로 살짝 축소되는데, `sea()`가 그 축소 변환 안에서
    그려져 화면 가장자리(바닥·좌우)가 안쪽으로 말려들어가 하늘색이 비쳐 보임 →
    바다 사각형을 화면 경계보다 넉넉히 크게(`MX`, `MYB` 여유분) 그려서 해결
12. `reset()`이 `holding`(팔 누름 상태)을 초기화하지 않아서, 이전 프레임에 누른
    채로 남아 있으면 다음 판 시작 시 `grab()`이 걸리지 않고 READY에 멈춰 있음 →
    `reset()`에 `holding = false` 추가. 테스트를 여러 개 이어 돌릴 때만 드러나던
    잠복 버그였음

## 검증 방법

브라우저 없이 스텁 DOM(가짜 `document`/`canvas`/`requestAnimationFrame`)을 만들어
Node에서 게임 루프를 직접 돌립니다.

```js
window.__swing = { peek, hold, pause, keys, pick, fire, reset, weak }
// peek() 에 mobs, skillCd, fist, deathReason 도 들어 있음
// fire() = 피스톨 발사, weak() = 현재 보스의 약점 배열
```

보스를 임의로 세워 놓고 약점을 확인하려면 하네스에서 IIFE 안쪽에 훅을 주입합니다.

```js
const HOOK = 'window.__test = { setBoss:(b)=>{boss=b}, wp:(b)=>weakPoints(b), P:()=>p, reset:()=>reset() };';
const i = src.lastIndexOf("})();");
vm.runInContext(src.slice(0,i) + HOOK + src.slice(i), sandbox);
```

- 봇이 "고리 아래를 지나 솟구칠 때 놓는" 정책으로 스윙하면 실제 플레이와 비슷하게 진행됩니다
- 새 기능을 넣으면 반드시 `node --check`로 문법 검사 후 봇으로 40판 정도를 돌려
  **원본과 나란히 비교**해 주세요. 한쪽만 돌리면 밸런스 붕괴를 못 잡습니다

### 현재 기준 (봇 40판, 보스는 무시하고 스윙만)

40판씩 여러 배치를 돌린 결과입니다. 편차가 크니 한 배치만 보고 판단하지 마세요.

| | 멈춤 | 중앙값 | 평균 | 최고 |
|---|---|---|---|---|
| 잡몹 도입 전 | 5~6/40 | 461~624m | 664~964m | 3145m |
| 현재 | 0~6/40 | 458~702m | 610~947m | 2739m |

사망 원인 분포는 바다 78~87% / 해왕류 13~22%입니다.

### 알려진 미해결 사항

- 줄이 `REEL_MIN`(74px)까지 감긴 채 고리를 계속 공전해 진행이 멈추는 판이
  나옵니다(40판에 0~6회, 배치마다 편차가 큼). **잡몹 도입 전부터 있던 현상**이고
  같은 조건에서 예전 버전도 5~6회 나옵니다. 봇이 놓을 타이밍을 못 잡아 생기는 쪽에 가까워 사람 플레이 영향은 적습니다.
  고치려면 `REEL_MIN`이나 자동 감기 로직을 손봐야 합니다.

## 배포 상태

- GitHub Pages: `https://aechamjae.github.io/Gomugomu/`
- 저장소: `aechamjae/Gomugomu` (Public)
- 구성: `index.html`, `manifest.json`, `sw.js`, `icon-180/192/512.png`
- PWA로 설치 가능(홈 화면에 추가), 서비스 워커로 오프라인 실행
- 최고 기록은 `window.storage` → 없으면 `localStorage` 순으로 저장
- 코드를 고친 뒤에는 `sw.js`의 캐시 버전(`gomupal-vN`)을 올려야 설치자에게 갱신이 전달됨
- **현재 캐시 버전: `gomupal-v21`**

## 재미 요소 추가 (불꽃 제한 · 보스 강제진행 · 칭호)

"게임이 단조롭다 / 보스가 너무 쉽다 / 보스 잡아도 보상이 없다"는 피드백으로 세 가지를 추가했다.

### 1. 불꽃(과속 무적) 2000m 제한

`FIRE_MAX_DIST = 2000`. 기존엔 속도가 `MAXV`를 순간적으로 넘기만 하면 언제든 불이 붙어
`knock()`/`knockKing()`의 피격 완화가 걸렸는데, 후반부까지 과속만 하면 사실상 무적으로
밀고 갈 수 있었다. `dist >= 2000`이면 아무리 과속해도 불이 붙지 않도록 트리거 조건에
거리를 추가했다(`sp > MAXV && dist < FIRE_MAX_DIST`).

### 2. 보스가 원거리 스나이핑으로 트리비얼하게 잡히는 문제

`firePistol()`이 사거리 860px에 속도·근접 조건 없이 약점 자동조준으로 명중해서,
멀리 매달려 쿨타임(5초)마다 쏘기만 해도 크라켄(HP 8) 포함 아무 보스나 안전하게 잡을 수
있었다. 두 가지를 함께 넣어야 실제로 막힌다(하나만 넣으면 우회됨 — 아래 "검증하며 발견한
것" 참고).

- **`PISTOL_BOSS_RANGE = 460`** — 피스톨이 보스 약점을 조준(`pistolAim`)하거나 실제로
  명중(`updateFist`)시킬 수 있는 거리를 460px로 제한. 잡몹·해왕류 조준 사거리(860)는
  그대로 둠. `test/checks.js`의 "피스톨이 보스 약점에 명중한다" 테스트가 620px 기준으로
  발사하던 걸 380px로 낮춰서 맞췄다.
- **정체 페널티(`STALL_*` 상수, 전역)** — 플레이어 x좌표가 `STALL_RESET_DX`(90px) 이상
  전진하지 못한 채 `STALL_WARN_T`(240프레임·4초)가 지나면 경고 배너, `STALL_PUSH_T`
  (330프레임·5.5초)가 지나면 카메라가 `STALL_PUSH_SPD`(0.85px/프레임)로 실제 전진해서
  기존 "화면 뒤로 밀려나면 사망"(`p.x < cam.x - 50`) 규칙에 걸리게 했다. 사인은 `"정체"`로
  따로 구분(게임오버 화면 문구도 다르게 나옴). 보스전뿐 아니라 게임 전체에 적용되는
  범용 장치라, 아무 데서나 너무 오래 멈춰 있으면 걸린다.
  펌핑하며 왔다갔다 하는 정상 스윙은 좌우 진폭만으로 90px를 넘나들어 타이머가 계속
  리셋되므로 거의 영향 없다.

#### 검증하며 발견한 것 (같은 실수 반복 방지)

1. **보스가 플레이어를 향해 알아서 다가온다.** 크라켄을 포함한 모든 보스는 `updateBoss()`
   안에서 플레이어와의 거리를 좁히려는 이동 로직을 갖고 있어서(예: 크라켄은
   `want = ... + ((p.x+290) - b.x) * 0.030`), 플레이어가 완전히 가만히 있어도 보스가
   알아서 사거리 안으로 걸어 들어온다. 그래서 피스톨 사거리 제한만으로는 "매달려서
   스나이핑" 자체를 막지 못하고(보스가 다가와 사거리 안에 들어와 버림), 정체 페널티가
   반드시 같이 있어야 한다.
2. **카메라 스프링 추적과 "밀어내기"를 더하면 평형점에서 멈춰버린다.** 처음엔
   `cam.x += 스프링항; if(정체) cam.x += PUSH;` 식으로 더했는데, 플레이어가 완전히
   고정되어 있으면 스프링의 복원력(계수 0.13, 거리에 비례)이 상수 푸시(0.85)와
   `PUSH/0.13 ≈ 6.5px` 지점에서 균형을 이뤄버려 카메라가 그 이상 못 따라잡고 정체가
   영원히 안 끝난다 — 정지 시나리오를 실제로 돌려서(`test/harness.js`에 임시로
   `stallT`/`cam` getter를 추가해) 확인했다. 고쳐서 정체 중엔 스프링을 아예 끄고
   `cam.x += PUSH*dt`만 적용하도록 분기했다. 완전히 정지한 상태 기준 경고(4초) →
   압박 시작(5.5초) → 실제 사망까지 약 12초 걸린다.
3. 위 두 가지를 테스트 없이 "상수만 추가하면 되겠지"로 넘겼다면 둘 다 실전에서
   무력화됐을 것 — 반드시 `test/harness.js`로 정지 시나리오를 직접 돌려서 사망까지
   가는지 확인해야 한다.

### 3. 보스 처치 칭호

`best`처럼 localStorage(`window.storage` 우선, 없으면 `localStorage`)에 영구 저장되는
`swing:titles` 레코드(`{ boss, kraken, unlocked:[id,...] }`)를 추가했다. 동료(코스메틱
따라다니는 비주얼)는 작업량 대비 우선순위가 낮아 이번엔 빼고 칭호만 넣었다.

- `TITLES` 배열(약한 순): 풋내기 사냥꾼(누적 보스 1) → 현상금 사냥꾼(누적 10) →
  크라켄 슬레이어(크라켄 1) → 그랑 라인의 폭풍(누적 30) → 심해의 재앙(크라켄 3) →
  해왕을 넘어선 자(크라켄 10). 배열 순서가 곧 "등급" — `currentTitleName()`은 이 순서로
  훑으며 마지막으로 해금된 걸 현재 칭호로 보여준다(두 트랙(누적/크라켄)이 섞여 있어서
  나중에 항목을 추가할 땐 순서로 등급을 조정하면 됨).
- `damageBoss()`에서 보스가 죽을 때(`b.hp<=0`) `lifeBossKills`/`lifeKrakenKills`를 올리고
  `checkTitles()`로 새로 해금된 게 있으면 팝업(`"칭호 획득: " + 이름`)을 띄운 뒤
  `saveTitles()`로 저장.
- 표시는 캔버스로 그리는 READY/OVER 화면에만 넣었다 — 데스크톱 HUD(`.hud`)는 터치
  기기에서 `display:none`으로 통째로 숨겨지는 구조라(`.mhud`가 대신 쓰임), 거기 넣으면
  모바일에서 안 보임. READY 화면엔 "현재 칭호", OVER 화면엔 이번 판에 새로 땄으면
  "★ 새 칭호 획득", 아니면 "현재 칭호"를 보여준다.

## 중간보스 4종 + 크라켄 밸런스 강화

"보스들이 너무 나약하다"는 피드백으로 보스별로 원래 있던 구멍을 하나씩 메웠다.

- **작살잡이(type 1)** — 예전엔 물속에 잠긴 동안(`b.air===false`) 완전히 무해했다.
  잠긴 동안 카운트다운(`b.harpoon`)을 새로 두고, 다 되면 `aimShot()`으로 작살을
  던진다(`kind:2`, `drawShots()`에 전용 렌더링 추가). 뛰어오르기 타이머(`b.fire`)와
  별도로 돌기 때문에 공중에 뜬 상태의 기존 위협과 겹쳐서 쉴 틈이 줄어든다.
- **포함 흑조호(type 2)** — 한 번에 한 발만, 66~116프레임마다 쏘던 걸 **한 번에
  세 발(T=42/52/62 세 궤적)을, 38~72프레임마다**로 바꿔서 훨씬 자주·훨씬 많이
  쏘게 했다.
- **곡예사 선장(type 0)** — 단검(`kind:0`)에 맞으면 스턴이 2배가 되도록
  `knock(hard, mild, mult)`에 `mult` 파라미터를 추가했었다. **이후 "모든 스턴시간을
  45프레임으로 통일" 요청으로 되돌림** — 자세한 건 아래 "스턴 시간 통일" 항목 참고.
- **밧줄 끊는 자(type 3)** — 원래 다음 고리 딱 하나만 끊어서, REACH(310px) 안에서
  그냥 넘어가지면 그만이라 사실상 무해했다. 이제 다음 고리 **두 개**를 한꺼번에
  끊어서(`b.perch`, `b.perch2`) 두 구간을 한 번에 건너뛰어야 하므로 미리 펌핑해서
  타이밍 맞춰 점프해야 한다. 재배치 주기도 300→260프레임으로 살짝 당겼다.
- **크라켄(type 4)** — 촉수가 `p.x + 200~520px`(플레이어보다 한참 앞)에서만
  솟아올라서, 가만히 있으면 촉수에 맞을 일이 영원히 없었다(2000m 제한 관련
  검증 과정에서도 이미 확인한 문제와 같은 종류). 촉수 생성 지점을
  `p.x + p.vx*14 ± 45px`로 바꿔 플레이어를 조준하게 하고, 살아있는 동안에도
  `tt.x += (p.x - tt.x) * 0.012 * dt`로 플레이어를 향해 계속 살금살금 다가가게
  했다(하네스로 직접 확인: 플레이어가 서서히 움직이면 촉수 x가 뒤따라 움직임).
  플레이어가 순식간에 멀리 점프하면(테스트에서만 가능한 상황) 기존 "너무
  뒤처지면 잠수해서 앞질러 나간다" 로직이 먼저 발동해 촉수가 리셋되는데, 이건
  원래부터 있던 재배치 로직이라 그대로 뒀다.

`test/checks.js`는 그대로 전부 통과하고(새 항목 추가는 안 함 — 보스 세부 공격
패턴은 하네스로 즉석 검증만 하고 지나갔다), `test/bot.js` 40판 평균도 기존
범위(458~557m) 안에서 유지된다.

## 작살잡이 추가 강화 — 작살 10발 부채꼴, 물고기 소환 스킬

"작살 10배, 물고기 생성 스킬 추가" 요청으로 작살잡이(type 1)를 한 번 더 강화했다.

- **작살 10발** — `b.harpoon` 쿨타임이 끝나면 한 발이 아니라 `aimShot()`을 10번 호출해
  부채꼴(`(i - 4.5) * 0.05`, 총 약 ±13°)로 10발을 한꺼번에 쏟아붓는다. 발사 주기 자체는
  그대로(50~90프레임)라 "한 발을 10발로" 해석했다 — 스턴 시간을 10배로 하는 쪽은 한 번
  맞으면 사실상 강제 사망이라 제외했다.
- **물고기 소환 스킬(신규)** — `b.fishSkill` 타이머를 새로 두고(스폰 시 200, 재배치
  사이클 종료 시 200으로 리셋), 160~240프레임마다 플레이어 앞쪽(`p.x+140~300`)에
  식인 물고기 3~4마리를 `spawnMobs()`와 같은 구조로 직접 `mobs.push()`한다. 잠수
  타이머(`b.harpoon`)와는 별개로 돌기 때문에 작살 세례와 물고기 떼가 겹칠 수 있다.
  `b.air`인 동안(공중에 뜬 도약 단계)엔 두 타이머 모두 멈춘다 — 하네스로 직접 돌려서
  실제로 카운트다운이 멈췄다 재개되는 것과, 460프레임 안에 물고기가 실제로 소환되는
  것까지 확인했다.

## 스턴 시간 통일 (STUN_FRAMES = 45)

"모든 스턴시간을 45프레임으로 통일해줘" 요청으로, 피격 종류마다 제각각이던 스턴
시간(hard 42 / mild 20 / base 30, 단검 2배 60, 해왕류 70·불꽃 중 36)을 전부
`STUN_FRAMES = 45` 상수 하나로 합쳤다.

- `knock(hard, mild)`에서 `mult` 파라미터(직전에 곡예사 단검 2배용으로 넣었던 것)를
  제거하고 `stun = STUN_FRAMES`로 고정. `updateShots()`의 단검 전용 `knock(false, false, 2)`
  호출도 `knock(false)`로 되돌렸다 — "스턴 2배"와 "스턴 전부 45로 통일"은 서로
  모순이라 나중 요청을 우선했다.
- `knockKing()`도 불꽃 중/아닌 두 분기 모두 `stun = STUN_FRAMES`로 통일. `p.vx` 감쇠율,
  `iframes`(피격 후 무적 프레임), 보물 손실 같은 **스턴 시간이 아닌** 세기 차이는
  그대로 뒀다 — 요청이 "스턴시간"만 콕 집었기 때문에 넉백 세기·무적시간까지 뭉개진
  않았다.
- 하네스로 단검/대포알/작살/잡몹/해왕류 다섯 가지 피격을 전부 돌려 `stun`이 정확히
  45로 나오는 것을 확인했다. `checks.js`의 "강한 스턴" 판정(`stun > 40`)은 45로도
  여전히 통과한다.

## 보스 등장 중엔 거리가 멈춘다

"보스 등장 시 그냥 쭉 진행하면 거리가 늘어나서" 피드백으로, 보스(중간보스·크라켄
둘 다)가 떠 있는 동안엔 앞으로 swing해도 `dist`가 늘지 않게 했다.

```js
if(!boss) dist = Math.max(dist, p.x / 22);   // 1321번째 줄 근처
```

- 보스가 살아있는 동안 `dist`를 아예 갱신하지 않는다(줄지도 않는다 — 그냥 마지막 값에서
  멈춤). 보스를 잡아서 `boss = null`이 되는 순간 다음 프레임부터 다시 `p.x/22`를
  따라잡는다(밀렸던 만큼 한 번에 점프해서 갱신됨 — 별도 보간 없음).
- `dist`를 쓰는 다른 곳(불꽃 2000m 제한, `nextBossAt`/`nextBigAt` 보스 재출현 임계값,
  최고기록 저장, HUD 표시)은 전부 `dist`가 프리즈되면 자연스럽게 같이 멈추는 게 맞는
  값들이라 손 안 댔다. 지형 생성(`generate()`)은 `dist`가 아니라 `cam.x`/`p.x`를 직접
  쓰기 때문에 영향 없음 — 보스 근처에서 계속 앞으로 가면 지형은 그대로 생성되지만
  거리 점수만 안 오른다.
- 하네스로 직접 확인: 보스 스폰 후 플레이어를 1000px 전진시켜도 `dist` 그대로,
  보스를 없애자마자 같은 프레임에 `p.x/22`로 즉시 따라잡음.
- `test/bot.js`의 단순 스윙 봇(보스를 제대로 못 잡고 그냥 지나치려는 정책)은 이제
  보스 앞에서 거리가 막히는 만큼 평균 거리가 좀 낮게 나온다(기존 458~557m →
  362~430m) — 봇이 보스를 못 잡아서 생기는 당연한 결과이고, 의도한 효과와 정확히
  같은 방향이라 밸런스 붕괴로 보지 않았다.

## 스턴 시간 재분화 — 해왕류·크라켄만 60프레임, 불꽃 중엔 절반

"스턴은 해왕류랑 크라켄만 60프레임, 불꽃효과=스턴시간절반" 요청으로 방금 통일했던
`STUN_FRAMES`를 다시 갈랐다. 완전히 되돌린 게 아니라 **딱 두 케이스만** 예외로 뒀다.

```js
const STUN_FRAMES = 45;  // 기본
const STUN_STRONG  = 60; // 해왕류·크라켄(촉수)만
```

- `knock(hard, mild)`의 `hard` 파라미터는 코드 전체에서 **크라켄 촉수 피격
  한 곳**(`updateBoss()`의 크라켄 분기, `knock(true)`)에서만 `true`로 들어온다는
  걸 확인하고, `hard ? STUN_STRONG : STUN_FRAMES`로 base를 계산했다. 보스 약점에
  느리게 닿거나(`knock(false)`) 잡몹에 스치거나(`knock(false, true)`) 탄에 맞는
  경우는 전부 `hard`가 false라 그대로 45.
- `knockKing()`(해왕류 접촉)은 `stun = STUN_STRONG`으로 직접 고정.
- **불꽃 효과**: `fire > 0`이면 base를 계산한 **뒤에** 절반으로 나눈다
  (`stun = fire > 0 ? base / 2 : base`) — 순서가 중요하다. 원래 있던
  "불붙으면 hard/mild를 강제로 mild로 낮춘다" 코드(넉백 세기·무적시간용)보다
  먼저 base를 구해야, 불붙은 채로 맞은 크라켄 촉수가 "약한 판정으로 낮아진
  45의 절반(22.5)"이 아니라 "원래 세기(60)의 절반(30)"이 된다. 순서를 반대로
  하면 조용히 틀린 값이 나오는 종류의 버그라 특히 주의.
- 하네스로 여섯 조합(단검/해왕류/크라켄 × 불꽃 없음/있음)을 전부 실측: 단검
  45→22.5, 해왕류 60→30, 크라켄 촉수 60→30. 불꽃은 `p.vx`를 순간적으로
  `MAXV` 이상으로 올려서(과속 무적 트리거) 실제로 켰다.

식인 물고기(`knock(false, true)`, 코드 전체에서 `mild=true`로 들어오는 유일한 곳)만
`STUN_MOB = 15`로 더 짧게 뺐다. `base = hard ? STUN_STRONG : mild ? STUN_MOB : STUN_FRAMES`
로 세 값을 한 줄에서 분기. 불꽃 중에 물고기에 부딪히면 애초에 `knock()`을 안 타고
(`updateMobs()`에서 `fire>0`이면 즉사+데미지 처리로 따로 빠짐, stun 관련 없음) 그대로
0인 게 맞다. 하네스로 물고기 단독 스턴 15, 단검(45로 변화 없음) 둘 다 확인.

## 보스 격파 직후 거리 점프 제거 (distOffset)

"보스전 중 거리 안 늘어나는 건 좋은데, 격파하면 그동안 이동한 거리가 한꺼번에
확 뛰어오른다"는 피드백. 이전 구현(`if(!boss) dist = Math.max(dist, p.x/22)`)은
보스가 살아있는 동안만 멈춰 있다가, 죽는 순간 `p.x/22`를 그대로 따라잡아서
싸우면서 이동한 만큼이 한 번에 점수로 들어갔다 — "멈춘다"보다는 "적립됐다가
한꺼번에 터진다"에 가까웠다.

```js
let distOffset;               // reset()에서 0으로 초기화
...
if(boss) distOffset = p.x / 22 - dist;             // 보스전 중엔 매 프레임 갱신
else dist = Math.max(dist, p.x / 22 - distOffset);  // 평소엔 오프셋만큼 뺀 값을 씀
```

- 보스가 떠 있는 동안 매 프레임 `distOffset`을 "지금 `p.x/22`가 멈춰 있는 `dist`보다
  얼마나 앞서 있는지"로 계속 갱신한다. 보스가 죽는 순간의 `distOffset`은 정확히
  `(격파 시점 p.x/22) - (멈춰 있던 dist)`가 되고, 그다음 프레임부터는 `p.x/22 - distOffset`이
  다시 멈춰 있던 `dist`와 같아지므로 점프가 없다 — 그 이후로 실제 전진한 만큼만
  `distOffset`을 빼고 남은 차이가 새로 쌓인다.
- `distOffset`은 게임 한 판 동안 보스전을 거칠 때마다 계속 누적되고, `reset()`에서만
  0으로 돌아간다(순차적으로 여러 보스를 잡아도 문제없이 이어짐).
- 하네스로 직접 확인: 보스 스폰 후 1000px 전진해도 `dist` 그대로 → 보스 제거 직후
  같은 x에서 `dist` 동일(점프 없음) → 그 뒤 100px 더 전진하면 정확히 `100/22`만큼만
  늘어남.

## 콤보(연속 스윙) 스타일 점수

"게임에 재미 요소가 더 필요하다"는 요청으로 세 가지를 한 번에 추가했다. 첫 번째는
연속 스윙에 대한 실질적인 점수 보상이다.

- 기존에도 `chain`(연속 성공 횟수)이 있었지만, 한 번 쌓이면 `knock()`으로 맞기 전까진
  절대 줄지 않아서 초반 몇 번만 성공하면 곧바로 배율(구 상한 ×4)이 고정돼 버렸다 —
  "콤보"라기보다 "한 번 얻으면 안 잃는 보너스"에 가까웠다.
- `CHAIN_DECAY_T = 200`(약 3.3초) 안에 다음 고리를 걸지 못하면 `chain`이 0으로 끊긴다.
  `chainTimer`를 매 프레임 누적하다가 성공적으로 걸 때(`airTime > 34`)마다 리셋한다.
  계속 실제로 스윙을 이어가야만 배율이 유지되는 구조로 바뀌었다.
- 배율 상한도 `CHAIN_CAP = 6`으로 올리고(구 4), `chainMult()` 함수 하나로 통일해
  **스윙 보너스뿐 아니라 금화 획득에도** 적용했다(`treasure += Math.round(25 * chainMult())`).
  보스·잡몹 처치 보상에는 일부러 적용하지 않았다 — "스타일 점수"는 스윙 흐름에 대한
  보상이라는 개념을 유지하려고, 전투 보상(격침 포상금 등)과는 분리해 뒀다.
- 화면 상단의 "연속 ×N" 표시에 단계별 명칭(NICE/GREAT/PERFECT!)과 색을 추가해 배율이
  오를수록 화면이 화려해지도록 했다.
- `best`(최고 거리)와 같은 패턴으로 `bestChain`을 `localStorage`(`swing:bestChain`)에
  영구 저장한다. READY 화면엔 "최고 콤보 ×N", OVER 화면엔 이번 판 최고 콤보와 신기록
  여부를 보여준다.
- 하네스에 `setChain` 훅을 추가해 검증했다: 콤보를 강제로 채운 뒤 매달린 채로
  `CHAIN_DECAY_T`보다 오래 대기하면 정확히 0으로 끊기는 것을 확인함(`test/checks.js`).

## 돌풍술사(신규 중간보스) — 물리 자체를 흔드는 보스

기존 중간보스 4종은 전부 "약점을 노려서 때린다"는 패턴이 같았다. 다섯 번째 중간보스는
공격 자체가 스턴이 아니라 **궤적을 강제로 밀어내는 돌풍**이라, 처음으로 "맞아도 안
아프지만 스윙 타이밍이 흐트러지는" 종류의 위협이 됐다.

- 기존엔 대형 보스(크라켄)가 하드코딩으로 `type 4`였다. 돌풍술사를 `type 4`에 끼워
  넣고 **크라켄을 `type 5`로 밀었다** — `spawnBoss()`의 `type=4`/`Math.random()*4`를
  `type=5`/`Math.random()*5`로 바꾼 것 말고는 `.big` 플래그로 분기하는 다른 코드는
  전혀 손대지 않았다(대형 보스 판정은 전부 `b.big`을 보지 숫자를 안 본다).
  `weakPoints()`/`updateBoss()`/`drawBoss()`의 `if(type===0)...else if(3){...} else{ //크라켄 }`
  디스패치 체인마다 크라켄으로 떨어지는 마지막 `else` 앞에 `else if(type===4)` 분기를
  끼워 넣었다 — 순서를 잘못 넣으면 조용히 돌풍술사가 크라켄 분기를 타 버리는 종류의
  실수라 하네스로 직접 약점 이름(`"돌풍술사"`, `"지팡이 끝"`)까지 확인했다.
- 동작: 곡예사 선장과 같은 부양 패턴(너무 뒤처지면 하늘로 솟아 앞에서 다시 내려옴)을
  쓰되, 공격은 단검이 아니라 `shots`에 `kind:3` 돌풍 탄을 하나 발사한다.
- **돌풍은 기존 `knock()` 경로를 타지 않는다.** `updateShots()`에서 `kind===3`이면
  스턴 대신 `p.vx += q.windPush`(좌우로 강하게 밀기)만 적용하고 끝낸다. 스턴·무적
  프레임을 하나도 건드리지 않아서, 기존에 공들여 맞춘 `STUN_*` 밸런스와 완전히
  독립적이다 — "닿으면 아프다"가 아니라 "닿으면 궤적이 흔들린다"는 별도의 위협.
  보호막(`shield`)도 이 경로에는 적용되지 않는다(애초에 피해가 아니라서 막을 게 없음).
- 시각: 발밑에 소용돌이 타원 3겹, 몸 옆에 반박자 늦게 흔들리는 지팡이 끝. 돌풍 탄
  자체도 소용돌이 궤적으로 그려서(`drawShots()`의 `kind===3` 분기) 다른 투사체와
  구분되게 했다.

## 보스 격파 랜덤 보상

"보스 잡으면 보상이 없다"는 예전 피드백에 칭호를 추가했었는데, 이번엔 **매번 격파할
때마다** 즉시 체감되는 보상을 하나 더 얹었다.

- `BOSS_REWARDS` 4종 중 격파 시점(`damageBoss()`의 `hp<=0` 분기, 칭호 판정 직후)에
  하나를 무작위로 골라 `applyBossReward(b)`로 적용한다.
  - **보호막** — `shield++`. 다음 피격 한 번을 완전히 무효화한다(스턴·넉백·보물 손실
    전부 없음). 플레이어 주위에 옅은 청록색 링으로 상시 표시(`drawShield()`).
  - **보물 폭풍** — `100 + bossKills*30` 즉시 지급.
  - **스킬 급속 재충전** — `skillCd`/`gunCd`를 0으로. 격파 직후 피스톨·건을 바로
    다시 쓸 수 있다.
  - **연속 콤보 부스트** — `chain`에 +3(상한 `CHAIN_CAP`까지). 위 콤보 시스템과
    직접 연결되는 보상.
- **보호막은 `knock()`과 `knockKing()` 양쪽 모두의 맨 앞에서 소비된다** —
  `stun>0||iframes>0` 가드 바로 다음, 실제 스턴 계산 이전에 가로챈다. 해왕류·크라켄
  촉수·보스 약점 근접 피격·잡몹 접촉 등 피해를 주는 경로가 전부 이 두 함수를 거치므로
  한 곳씩 따로 막을 필요 없이 여기 두 곳만 손보면 됐다. 막아낸 피격은 `chain`을
  리셋하지 않는다(애초에 `knock()`의 스턴 분기 자체를 안 타므로) — 보호막으로 버틴
  타격이 콤보를 안 끊는 건 의도한 시너지.
- 하네스로 확인: 보호막 1개를 쥔 채 식인 물고기에 닿으면 `shield`가 0으로 줄고
  `stun`은 0을 유지한다(스턴이 아예 발생하지 않음). 보스를 HP 1로 만들어 40번
  반복 격파시키면 보호막 보상이 최소 한 번은 나온다(`test/checks.js`).

## 이번 작업 공통 — 테스트 인프라 확장

세 기능 모두 실제 게임 로직에 훅을 걸어 검증해야 해서 `test/harness.js`의
`window.__test`에 `setShield`, `setChain`, `pushShot`, `dmg`(=`damageBoss` 직접 호출)를
추가했다. `test/checks.js`도 보스 타입 번호가 밀린 것(4→5)에 맞춰 약점 테스트의
타입 배열을 갱신하고, 새 기능 다섯 개(보호막 차단, 돌풍 밀어내기, 콤보 유지/감쇠,
보스 보상 확률)에 대한 검사를 추가했다. `node test/checks.js` 전부 통과, `node
test/bot.js 10800 40`으로 수정 전/후를 나란히 두 번씩 돌려 평균 거리(381~422m 범위)가
기존 변동폭 안에 있음을 확인했다 — 새 보스의 돌풍은 스턴이 없어서 밸런스에 미치는
영향이 적고, 보호막·재충전 보상은 죽음의 주 원인(바다 78~87%)과 직접 관련이 없어
평균에 큰 변화를 주지 않았다.

## 본게임 / 연습 모드 메뉴

"게임 시작 전에 본게임과 연습게임을 나누고, 연습은 보스를 골라서 하거나 해왕류가
개많이 나오는 주행 같은 걸 고를 수 있게 해달라"는 요청으로 시작 화면을 새로 만들었다.

- 새 상태 `STATE.MENU`(4)·`STATE.PRACTICE_SELECT`(5)를 기존 READY(0)/PLAY(1)/OVER(2)/
  PAUSE(3) 뒤에 추가했다. **기존 숫자를 안 건드린 이유**: `test/checks.js`·`test/bot.js`가
  `state === 2`로 OVER를 리터럴 비교하는 곳이 있어서, 기존 값을 바꾸면 테스트가 전부
  깨진다.
- **핵심 설계: 메뉴는 `reset()` 앞에 붙는 얇은 막이다.** `reset()` 자체는 여전히 항상
  `state = STATE.READY`로 끝난다 — 메뉴 로직을 `reset()` 안에 넣지 않고, "메뉴에서 선택 →
  `practiceMode` 설정 → `reset()` 호출"이라는 흐름으로 바깥에 얹었다. 덕분에
  `window.__swing.reset`(모든 테스트가 쓰는 훅)을 직접 호출하는 기존 테스트는
  **단 한 줄도 안 고쳤는데 그대로 통과한다** — 메뉴를 거치지 않고 바로 READY로 들어가기
  때문이다. 부팅 시퀀스만 `reset(); state = STATE.MENU;`로 바꿔서 최초 화면만 메뉴가
  뜨게 했다.
- `practiceMode`는 `{ type:"boss", bossType:N }` 또는 `{ type:"king" }`(본게임은 `null`)
  이고, **`reset()`이 초기화하지 않는 전역 변수**다 — R로 재시작해도 같은 연습 내용을
  유지하기 위해 일부러 뺐다. `startMainGame()`/`startPractice(idx)` 두 함수만 이 값을
  바꾸고, 둘 다 마지막에 `reset()`을 호출한다.
- **메뉴 선택지는 `menuRows()` 배열 하나로 그리기·클릭·숫자키가 전부 같은 소스를 본다**
  (좌표를 두 군데에 따로 적으면 클릭 판정이 그림과 어긋나는 실수가 나기 쉬워서 함수
  하나로 합쳤다). 숫자 키(Digit1~9)와 캔버스 클릭(`canvasPos()`로 CSS 스케일을 보정한
  좌표) 둘 다 이 배열을 순회해서 같은 `action()`을 실행한다.
- **연습별 실제 동작**:
  - 보스 연습(`type:"boss"`) — `reset()`에서 해당 보스 슬롯(중간보스는 `nextBossAt`,
    크라켄은 `nextBigAt`)만 30m로 당기고 반대쪽은 `Infinity`로 꺼서 그 보스만 빨리
    나오게 했다. `spawnBoss()`는 `practiceMode.bossType`이 있으면 무작위 선택 대신
    그 타입을 강제한다. `spawnMobs()`는 보스 연습 중엔 **아무 잡몹도 안 뿌리고
    즉시 return**해서 방해 요소를 없앴다. 격파 후에는 `damageBoss()`에서 같은 슬롯을
    `dist+60`으로 다시 당겨서 짧은 간격으로 반복 등장한다.
  - 해왕류 폭주(`type:"king"`) — `reset()`에서 두 보스 슬롯을 전부 `Infinity`로 꺼서
    일반 보스가 안 나오게 하고, `spawnMobs()`에 전용 분기를 추가해 최소 거리 제한 없이
    480px 간격마다 85% 확률로 해왕류를 쏟아낸다(기존 2600px/11%와 비교하면 훨씬
    촘촘함). 식인 물고기는 이 분기에서 아예 안 뿌린다.
- **연습 모드는 영구 기록을 오염시키지 않는다.** `damageBoss()`의 `lifeBossKills`/
  `lifeKrakenKills` 증가·칭호 판정·`saveTitles()`와 `die()`의 `best`/`bestChain` 저장을
  전부 `if(!practiceMode)`로 감쌌다. 보스 연습에서 같은 보스를 100번 잡아도 칭호
  진행도가 그대로인 걸 하네스로 직접 확인했다(`test/checks.js` "연습 모드에서
  격파해도..." 항목). 보스 격파 랜덤 보상(보호막 등)은 세션 한정 재미 요소라 연습
  중에도 그대로 받는다 — 의도적으로 안 막았다.
- 키보드 **M**을 새 전역 단축키로 추가해 PLAY 도중에도 즉시 메뉴로 돌아갈 수 있다
  (죽음 처리 없이 그냥 런을 버리고 나간다). PRACTICE_SELECT 화면에서 **Esc**는 메뉴로
  뒤로가기. READY/OVER 화면 안내 문구에도 "M 메뉴로"를 추가했다.
- `grab()`은 메뉴 상태에서 완전히 no-op이다 — 터치 기기의 "팔" 버튼이나 Space를 눌러도
  메뉴에선 아무 일도 안 생기고, 오직 캔버스 클릭/터치와 숫자 키로만 선택한다(터치
  오버레이 버튼들은 화면 가장자리에 있고 메뉴 글자는 화면 중앙에 그려서 겹치지 않는다).
- 하네스에 `startMain`/`startPractice`/`toMenu`를 노출했다(`window.__swing`). 키보드
  이벤트는 하네스의 `window.addEventListener`가 빈 스텁이라 시뮬레이션이 안 되므로,
  다른 스킬(Q/W)처럼 함수를 직접 호출하는 방식으로 테스트했다. `lifeStats()` 훅도
  추가해 칭호 진행도 오염 여부를 검증했다.
- `test/bot.js`는 여전히 `reset()`을 직접 호출해 본게임 기준으로 도니까(메뉴를
  거치지 않음) 밸런스 벤치마크에는 영향이 없다 — 40판 평균 369m로 기존 변동폭
  안에서 유지되는 것을 확인했다.

## 남은 아이디어

- 순위표(서버 필요 — GitHub Pages 단독으로는 불가)
- 난이도 선택 (고리 간격·보스 주기·제한시간을 프리셋으로 묶고 기록도 난이도별 분리)
- 피스톨 충전 방식을 시간제에서 금화 수집제로 전환
- 고리를 옮기는 계열 보스 — 바람 계열(돌풍술사)은 이번에 추가했으니 다음은 이쪽
- 잡몹 추가 — 지금은 물속에서 오는 위협뿐이라 공중 잡몹이 비어 있음
- 동료(코스메틱) 시스템 — 칭호처럼 마일스톤으로 해금하되, 플레이어를 따라다니는 비주얼을
  외부 이미지 없이 캔버스로 직접 그려야 해서 작업량이 큼. 이번 작업에서 범위를 좁혀 뺐음
- 보스 보상 4종은 전부 같은 확률(25%)이다 — 보물 폭풍처럼 흔해도 되는 것과 보호막처럼
  강한 것의 확률을 다르게 주는 가중치 뽑기로 바꿀 수 있음
